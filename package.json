{
  "name": "telegram-deployer",
  "version": "1.0.0",
  "description": "Telegram bot that deploys user websites",
  "main": "server.js",
  "scripts": {
    "start": "npx pm2 start ecosystem.config.js --no-daemon"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "pm2": "^5.3.0"
  }
}
